<?xml version="1.0" encoding="UTF-8"?>

<!-- $Id$ -->

<!DOCTYPE project [
	  <!ENTITY % locator-entities SYSTEM "file:locator.ent">
	  %locator-entities;
	  ]>

<project>
  <extend>${basedir}/../../project.xml</extend>
  <name>Moria Directory manager</name>
  <id>moria-dm</id>
  <currentVersion>1.1</currentVersion>
  <package>no.feide.moria.directory</package>

  <shortDescription>This is the Moria Directory manager</shortDescription>

  <description>This is the directory manager of Moria, handling
    communication with external directories (LDAP) and indexing of
    users.</description>

  <siteDirectory>/home/groups/m/mo/moria/htdocs/moria-dm</siteDirectory>

  <dependencies>
    &dep-jdom;
    &dep-moria-log;
    &dep-log4j;
  </dependencies>

</project>
